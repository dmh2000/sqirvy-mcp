# MCP Server Configuration

# Logging configuration
log:
  # Log level (DEBUG, INFO)
  level: INFO
  # Path to log file
  output: sqirvy-mcp.log

# Project configuration
project:
  # Root path for file resources
  rootPath: resources

# Tools configuration
tools:
  online:
    # Target IP for online tool
    target: 8.8.8.8
